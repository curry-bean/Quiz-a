import { ChangeStyleDirective } from './change-style.directive';

describe('ChangeStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeStyleDirective();
    expect(directive).toBeTruthy();
  });
});
