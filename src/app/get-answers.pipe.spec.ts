import { GetAnswersPipe } from './get-answers.pipe';

describe('GetAnswersPipe', () => {
  it('create an instance', () => {
    const pipe = new GetAnswersPipe();
    expect(pipe).toBeTruthy();
  });
});
